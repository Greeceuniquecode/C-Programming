// this is a mixed max calculation of 2 number
#include <stdio.h>
#include <conio.h>
int main()
{
    float a, b;
    printf("enter two number idiot: ");
    scanf("%f%f", &a, &b);
    printf("the sum is %f .\n", a + b);
    printf("the difference is %f\n", a - b);
    printf("the muntiplication is %f\n", a * b);
    printf("the division is %f", a / b);
    return 0;
}